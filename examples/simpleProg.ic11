pin Device db;

void Main()
{
    while(true)
    { 
        yield;
        
        var a = 3;
        var b = 5;

        Device.Setting = Calculate(a, b);
    }
}

real Calculate(a, b)
{
    return a + a + b;
}