pin DialElectro d3;

const b = 3;

void Main()
{
    while(true)
    {
        yield;

        var a = 0;
        
        if (DialElectro.IsSet)
            a = GetSetting(DialElectro.ReferenceId);
        
        if (true)
            Base.Setting = a + b;
    }
}

real GetSetting(dialId)
{
    return DeviceWithId(dialId).Setting;
}