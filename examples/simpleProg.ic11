pin MyDevice d3;

void Main()
{
    while(true)
    { 
        yield;
        
        var a = 3;
        var b = 5;

        Base.Setting = Calculate(a, b);
        var c = MyDevice.Setting;
    }
}

real Calculate(a, b)
{
    return a + a + b;
}